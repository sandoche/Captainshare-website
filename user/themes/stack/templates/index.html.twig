{% extends 'partials/base.html.twig' %}

{% block stylesheets %}
  {% do assets.addCss('theme://css/index.css') %}
{% endblock %}

{% block body %}
<section class="container">
  <div class="row">
    {% for page in pages.children.visible %}
        {% if page.active != 1 %}
          <a href="{{ page.url }}" class="col-md-3 category">
              {% if page.header.icon %}<i class="fa fa-{{ page.header.icon }}"></i>{% endif %}
              {{ page.menu }}
          </a>
        {% endif %}
    {% endfor %}
  </div>
</section>
{% endblock %}
